{
  "name": "medlens-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "fastify": "^4.24.0",
    "@google-cloud/firestore": "^6.12.0",
    "@google-cloud/storage": "^7.12.0",
    "firebase-admin": "^11.12.0",
    "fastify-multipart": "^7.0.0",
    "fastify-helmet": "^8.0.0",
    "fastify-rate-limit": "^7.0.0",
    "pino": "^8.20.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "ts-node-dev": "^2.0.0"
  }
}
